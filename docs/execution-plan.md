# 执行计划 — 极简记账

## 阶段总览

| 阶段 | 名称 | 核心产出 | 预估工作量 |
|------|------|----------|------------|
| 1 | 项目初始化 | 文件夹、文档体系、AGENTS.md | ✅ 当前 |
| 2 | 页面骨架+样式 | index.html 结构 + CSS变量系统 | ✅ 已完成 |
| 3 | 数据层 | localStorage 工具函数 + 预设数据 | ✅ 已完成 |
| 4 | 记账功能 | 记账表单 + 分类管理 CRUD | ✅ 已完成 |
| 5 | 账单展示 | 账单卡片列表 + 筛选搜索 | ✅ 已完成 |
| 6 | 月度统计 | 概览卡片 + 分类占比图 | ✅ 已完成 |
| 7 | 收尾打磨 | 空状态、删除确认、最终测试 | ✅ 已完成 |

---

## 阶段 1：项目初始化（当前）

### 交付物
- [x] 创建 `devlog/` 文件夹
- [x] 创建 `docs/` 文件夹
- [ ] 编写 `AGENTS.md`
- [ ] 编写 `docs/requirements.md`
- [ ] 编写 `docs/tech-spec.md`
- [ ] 编写 `docs/design-spec.md`
- [ ] 编写 `docs/execution-plan.md`
- [ ] 编写 `devlog/2026-07-10.md`

---

## 阶段 2：页面骨架 + 样式系统

### 目标
搭建 `index.html` 的基础 HTML 结构和完整 CSS 样式系统。

### 交付物
- `index.html` 包含：
  - 完整 HTML 骨架（顶栏、概览区、Tab导航、4个内容面板）
  - `:root` CSS 变量定义
  - 全局样式重置
  - 布局样式（最大宽度720px居中）
  - 概览卡片样式
  - Tab 导航样式
  - 响应式适配（`@media max-width: 480px`）

### 验收标准
- 页面在浏览器中正常渲染
- 4个Tab可点击切换（视觉状态变化）
- 概览卡片展示占位数据
- 配色方案符合设计规范

---

## 阶段 3：数据层

### 目标
实现完整的数据持久化层。

### 交付物
- `localStorage` 读写工具函数
- 预设14个分类初始化逻辑
- 账单 CRUD 函数（增删改查）
- 分类 CRUD 函数
- 月份/分类筛选查询函数
- 月度汇总统计函数

### 验收标准
- 首次打开自动初始化预设分类
- 可在浏览器 DevTools 中查看 `jz_categories` 和 `jz_bills`
- CRUD 操作正确读写 localStorage

---

## 阶段 4：记账功能

### 目标
实现完整的记账表单和分类管理。

### 交付物
- 记账表单（类型切换、金额、分类下拉、日期、备注）
- 表单验证逻辑
- 提交保存 + 成功反馈
- 分类管理面板（列表 + 新增/编辑/删除弹窗）
- Tab 切换到"记账"和"分类"的内容渲染

### 验收标准
- 可以成功记录一条账单
- 可以在分类管理中新增/编辑/删除分类
- 删除有账单的分类时给出提示
- 页面刷新后数据不丢失

---

## 阶段 5：账单展示

### 目标
实现账单卡片列表、筛选和搜索。

### 交付物
- 账单卡片渲染（收入绿/支出橙红）
- 按日期倒序排列
- 月份筛选器
- 分类筛选器
- 关键词搜索
- 删除/编辑单条账单
- Tab 切换到"账单"的内容渲染

### 验收标准
- 账单卡片视觉区分明显
- 筛选和搜索功能正确
- 删除操作有确认弹窗
- 编辑功能可修改已有账单

---

## 阶段 6：月度统计

### 目标
实现月度汇总和分类占比展示。

### 交付物
- 顶部概览卡片动态计算（当月收入/支出/结余）
- CSS进度条展示各分类占比
- 月份切换器联动统计更新
- Tab 切换到"统计"的内容渲染

### 验收标准
- 概览数字与账单数据一致
- 进度条比例正确
- 切换月份统计数据正确更新

---

## 阶段 7：收尾打磨

### 目标
完善体验细节，确保软件可用。

### 交付物
- 空状态引导页（账单为空 / 筛选无结果）
- 自定义确认弹窗组件
- 数据清除功能（需二次确认）
- 金额输入格式化（保留两位小数）
- 全面功能测试

### 验收标准
- 所有功能流程通畅
- 无明显的视觉bug
- 在Chrome和Edge中都能正常运行
